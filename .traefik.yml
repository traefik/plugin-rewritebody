displayName: Rewrite Body Gzip
summary: Built on top of Traefik Rewrite Body plugin to also support gzip decompress -> update -> recompress.
type: middleware
import: github.com/packruler/plugin-rewritebody
compatibility: TODO
testData:
  lastModified: true
  rewrites:
    - regex: "bar"
      replacement: "foo"
